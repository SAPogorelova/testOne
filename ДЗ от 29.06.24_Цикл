Цикл 

# 1. Подсчет суммы чисел:
# Напишите программу, которая считает сумму чисел от 1 до N, где N задается пользователем.

Ответ:

Number = int(input("Введите число:"))
a = 1
b = 0
while a <= Number:
    print(a)
    b = b + a
    a = a + 1
print(b)

----------------------------------------------------------------------------------------------------

# 2. Поиск суммы 2:
# Напишите программу, которая запрашивает у пользователя числа до тех пор,
# пока не будет введен 0, и находит сумму из введенных чисел.

Ответ:

print("Угадай число: ")
N = 0
sum = 0
# задаем значения N = 0 и для подсчета суммы введенных чисел

while True:
    number = int(input())
# бесконечный цикл While. Программа запрашивает у пользователя число и сохраняет его в переменную Number
    if number == N:
        print('Вы угадали. Поздравляем.')
        break
# Проверка введенного чила. Если введенное число равно 0 (переменной N), то печатется поздравление и программа завершается
    else:
        print ('Попробуйте еще раз.')
# в противном случае, если число отличное от 0, то предлагается попробовать еще раз

    sum += number
print(f"Сумма введенных чисел: {sum}")
# подсчет и печать суммы введенных чисел из попыток

-------------------------------------------------------------------------------------------------------

# 3. Факториал числа:
# Напишите программу, которая вычисляет факториал заданного числа N.

Ответ:

Number = int(input("Введите число: "))
# Ввод числа от пользователя

factorial = 1
# Инициализация факториала

for i in range(1, Number + 1):
    factorial *= i
# Вычисление факториала числа N


print(f"Факториал числа {Number} равен {factorial}")

-------------------------------------------------------------------------------------------------------
# 4. Проверка на простое число:
# Напишите программу, которая проверяет, является ли введенное пользователем число простым.

Ответ:


number = int(input("Введите число: "))
# Ввод числа от пользователя


def is_prime(num):
    if num <= 1:
        return False
    for i in range(2, int(num**0.5) + 1):
        if num % i == 0:
            return False
    return True
# Функция для проверки, является ли число простым


if is_prime(number):
    print(f"{number} является простым числом.")
else:
    print(f"{number} не является простым числом.")
# Проверка и вывод результата
